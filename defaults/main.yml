---
k8s_init_cidr: "192.168.0.0/16"  # good default, calico uses it
# k8s_init_node_ip: "192.118.19.10"  # ip it exposes, must be unique for each node
# k8s_init_node_name: "{{ inventory_hostname }}"
